// J.A.R.V.I.S. Client Configuration
// No secrets here — the API key lives in Vercel's environment variables.
// See api/ask.js for the server-side proxy.

const JARVIS_CONFIG = {
  askEndpoint: "/api/ask",
};
